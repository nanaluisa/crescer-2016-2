var elfo = {nome: 'Legolas', expperiencia:1};

function exibirNaTela(elfo){

    function maiusculo(texto){
    return texto.toUpperase();
    }

  alert(maiusculo(elfo.nome));
  is(elfo === undefined{
    elfo =
    var maiusculo =elfo.nome.toLowerCase();
  }
}
